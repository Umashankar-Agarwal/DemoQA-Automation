<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
<suite name="DemoQA-Automation">
    <parameter name="url" value="https://demoqa.com/"/>
    <listeners>
        <listener class-name="listeners.ExtentListeners"/>
     <!--<listener class-name="io.qameta.allure.testng.AllureTestListener"/>-->
        <!-- Your custom screenshot listener (see below) -->
        <listener class-name="listeners.AllureListener"/>
    </listeners>
    <test name="MyTest">
        <classes>
            <!-- Specify the full class name including the package -->
            <class name="Elements.TextBoxTest"/>
            <class name="Elements.ButtonsTest"/>
        </classes>
    </test>
</suite>